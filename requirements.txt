# Pinned exactly. Open ranges meant every deploy resolved fresh versions, so a
# compromised release of any of these would land straight in a process that holds
# the bot token and the Supabase key in memory.
# Bump deliberately: change a version, redeploy, verify.
python-telegram-bot[job-queue]==22.8
Flask==3.1.3
pytz==2026.2
tzdata==2026.2
python-dotenv==1.2.2
# google-generativeai  (disabled to save memory)
pillow==12.2.0
supabase==2.31.0
httpx==0.28.1
gunicorn==26.0.0
psutil==7.2.2
